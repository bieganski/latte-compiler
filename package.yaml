name: latte-compiler
version: '1.0'
author: Mateusz Biegański

executables:
  latc:
    main:          Main.hs
    source-dirs:   src
dependencies:
  - base >= 4.7 && < 5
  - text
  - array
  - containers
  - mtl
  - filepath
  - process
  - Unique == 0.4.7.6
  - lens
